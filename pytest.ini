[pytest]
addopts = -ra -q --maxfail=0 --durations=25 --strict-markers
testpaths = tests
markers =
    integration: tests that require live services (NATS/Prometheus/etc.)
    slow: long-running functional tests
    asyncio: async test functions
