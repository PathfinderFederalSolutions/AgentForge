apiVersion: v1
kind: Service
metadata:
  name: agent-swarm-frontend
  namespace: web
  labels: { app: agent-swarm-frontend }
spec:
  type: ClusterIP
  selector:
    app: agent-swarm-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
