apiVersion: v1
kind: Service
metadata:
  name: tactical-dashboard
  namespace: agentforge-staging
spec:
  selector: { app: tactical-dashboard }
  ports:
    - name: http
      port: 80
      targetPort: 80
---
